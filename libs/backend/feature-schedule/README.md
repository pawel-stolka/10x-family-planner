# feature-schedule

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build feature-schedule` to build the library.

## Running unit tests

Run `nx test feature-schedule` to execute the unit tests via [Jest](https://jestjs.io).
