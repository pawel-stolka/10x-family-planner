import { FrontendFeatureAuth } from './frontend-feature-auth';

describe('FrontendFeatureAuth', () => {
  it('should create an instance', () => {
    const component = new FrontendFeatureAuth();
    expect(component).toBeTruthy();
  });
});
